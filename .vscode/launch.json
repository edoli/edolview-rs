{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Rust (cppvsdbg)",
      "type": "cppvsdbg",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/edolview-rs.exe",
      "args": ["image.png"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "preLaunchTask": "cargo build"
    }
  ]
}
